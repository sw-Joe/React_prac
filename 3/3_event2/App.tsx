import Button from "./components/Button";

export default function App() {
    return (
        <>
            <Button name={'Home'} />
            <Button name={'Store'} />
            <Button name={'Contact'} />
        </>
    )
}